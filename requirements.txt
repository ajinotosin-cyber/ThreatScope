# Runtime dependencies for the Streamlit app (app.py and everything it
# imports). Kept to what is genuinely used -- see README "Requirements
# audit" for what was removed and why.
streamlit>=1.32
pandas
plotly
requests
python-dotenv
yara-python
